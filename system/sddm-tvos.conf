# Lets SDDM see sessions installed under /usr/local — needed on immutable
# distros like Bazzite where /usr/share is read-only.
[Wayland]
SessionDir=/usr/share/wayland-sessions,/usr/local/share/wayland-sessions
